#include <bits/stdc++.h>
#define endl "\n"
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string s;
    getline(cin, s);
   	int dem = 0;
	for (int i = 0 ; i + 4 < s.size() ; i++)
		dem += (s.substr(i , 5) == "virus");
	cout << dem << endl;
    return 0;
}
